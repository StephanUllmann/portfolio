<a href="/stephanullmann_cv.pdf" download class="print-button">&DownArrowBar;</a
>

<style>
  @property --amount {
    syntax: "<percentage>";
    inherits: true;
    initial-value: 50%;
  }
  .print-button {
    display: grid;
    place-content: center;
    position: fixed;
    bottom: 2rem;
    right: 2rem;
    font-size: 1.75rem;
    width: 2.5rem;
    aspect-ratio: 1;
    background-color: var(--base);
    color: var(--accent);
    border: none;
    border-radius: 2rem;
    cursor: pointer;
    &::before {
      --amount: 50%;
      content: "";
      background-image: radial-gradient(
          var(--base) var(--amount),
          transparent 90%
        ),
        linear-gradient(
          var(--deg, 135deg) in oklch,
          var(--primary),
          var(--accent)
        );
      border-radius: 2rem;

      position: absolute;
      z-index: -1;
      inset: -3px;
      transition: all 300ms linear;
    }
    &:hover {
      color: color-mix(in oklch, var(--accent), #fff 15%);
    }
    &:hover::before {
      --amount: 40%;
      transition: all 300ms linear;
    }
  }
</style>
